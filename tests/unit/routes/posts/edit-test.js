import {
  moduleFor,
  // test 
} from 'ember-qunit';

moduleFor('route:posts/edit', 'Unit | Route | posts/edit', {
  needs: ['service:session']
});

// test('it exists', function(assert) {
//   let route = this.subject();
//
//   assert.equal(currentRouteName(), 'posts.index');
//   assert.ok(route);
// });
