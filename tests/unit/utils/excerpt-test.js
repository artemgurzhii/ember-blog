import excerpt from 'ember-blog/utils/excerpt';
import { module, test } from 'qunit';

module('Unit | Utility | excerpt');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = excerpt();
  assert.ok(result);
});
