import formatPostUrl from 'ember-blog/utils/format-post-url';
import { module, test } from 'qunit';

module('Unit | Utility | format post url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatPostUrl();
  assert.ok(result);
});
