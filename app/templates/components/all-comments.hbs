{{#each comments as |comment|}}

  <!-- Make sure record exist -->
  {{#if comment.id}}
    {{blog-post-comment comment=comment}}
  {{/if}}
{{else}}
  <h2>No Comments were found</h2>
  <h3>Create first comment</h3>
{{/each}}
